name: Capture production audit

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  capture:
    runs-on: ["forgejo-container"]
    container:
      image: node:24-bookworm
    env:
      TARGET_BRANCH: fix/next-16.3.4-audit
    steps:
      - name: Fetch exact repair branch
        shell: sh
        run: |
          set -eu
          apt-get update
          apt-get install -y --no-install-recommends git
          git config --global --add safe.directory "$PWD"
          git init .
          git remote add origin "${FORGEJO_SERVER_URL:-${GITHUB_SERVER_URL}}/${FORGEJO_REPOSITORY:-${GITHUB_REPOSITORY}}.git"
          token="${FORGEJO_TOKEN:-${GITHUB_TOKEN:-}}"
          test -n "${token}"
          git -c http.extraHeader="Authorization: token ${token}" fetch --depth=1 origin "refs/heads/${TARGET_BRANCH}"
          git checkout -B "${TARGET_BRANCH}" FETCH_HEAD

      - name: Install locked dependencies
        shell: sh
        run: npm ci --no-audit --no-fund

      - name: Capture production audit JSON
        shell: sh
        run: |
          set +e
          npm audit --omit=dev --json > docs/NPM_AUDIT_TMP.json
          rc=$?
          set -e
          echo "audit_exit_code=${rc}" > docs/NPM_AUDIT_EXIT_TMP.txt

      - name: Commit audit evidence and self-remove
        shell: sh
        run: |
          set -eu
          rm -f .forgejo/workflows/capture-audit.yml
          git config user.name "forgejo-actions"
          git config user.email "forgejo-actions@users.noreply.forgejo.tca-infraforge.site"
          git add docs/NPM_AUDIT_TMP.json docs/NPM_AUDIT_EXIT_TMP.txt .forgejo/workflows/capture-audit.yml
          git diff --cached --check
          git commit -m "chore(ci): capture production audit evidence"
          token="${FORGEJO_TOKEN:-${GITHUB_TOKEN:-}}"
          test -n "${token}"
          git -c http.extraHeader="Authorization: token ${token}" push origin "HEAD:refs/heads/${TARGET_BRANCH}"
