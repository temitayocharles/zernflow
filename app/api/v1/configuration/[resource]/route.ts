import {ApiError,databaseError,failure,json,productContext} from '@/lib/product/api';import {configResource,ownerConfiguration,parseConfiguration} from '@/lib/product/config-contracts';import type {Database} from '@/lib/types/database';
export async function POST(request:Request,{params}:{params:Promise<{resource:string}>}){try{const {resource}=await params;if(!configResource(resource))throw new ApiError(404,'Unknown resource');const {supabase,workspaceId,role}=await productContext();if(ownerConfiguration(resource)&&role!=='owner')throw new ApiError(403,'Workspace owner required');const input=parseConfiguration(resource,await request.json());const {data,error}=await supabase.from(resource).insert({...input,workspace_id:workspaceId} as Database['public']['Tables'][typeof resource]['Insert']).select().single();databaseError(error);return json(data,201);}catch(e){return failure(e);}}
