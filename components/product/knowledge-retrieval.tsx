"use client";import {useState} from 'react';import type {RetrievalResult} from '@/lib/knowledge/contracts';
export function KnowledgeRetrieval({sources}:{sources:{id:string;name:string}[]}){const [result,setResult]=useState<RetrievalResult|null>(null);const [error,setError]=useState<string|null>(null);const [busy,setBusy]=useState(false);return <form className="space-y-4" onSubmit={async e=>{e.preventDefault();setBusy(true);setError(null);setResult(null);const form=new FormData(e.currentTarget);try{const r=await fetch('/api/v1/knowledge/retrieve',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:form.get('query'),sourceIds:form.getAll('sources')})});const data=await r.json();if(!r.ok)throw new Error(data.error);setResult(data);}catch(e){setError(e instanceof Error?e.message:'Retrieval failed');}finally{setBusy(false);}}}><fieldset><legend className="text-sm font-medium">Enabled sources</legend>{sources.map(s=><label key={s.id} className="mt-2 block text-sm"><input type="checkbox" name="sources" value={s.id}/> {s.name}</label>)}{!sources.length&&<p className="text-sm text-muted-foreground">No enabled sources. Ask the workspace owner to configure one.</p>}</fieldset><label className="block text-sm">Retrieval query<textarea name="query" required maxLength={4000} className="mt-2 w-full rounded border border-border bg-background p-3"/></label><button disabled={busy||!sources.length} className="rounded bg-primary px-4 py-2 text-sm text-primary-foreground disabled:opacity-50">{busy?'Retrieving…':'Retrieve context'}</button>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}{result&&<section className="space-y-3"><h2 className="font-semibold">Source context · Indexing {result.indexingStatus}</h2><p className="text-xs text-muted-foreground">External request {result.requestId}. These are retrieved excerpts, not an AI-generated answer.</p>{result.citations.map((c,i)=><article key={i} className="rounded border border-border p-4"><h3 className="font-medium">{c.title}</h3><p className="whitespace-pre-wrap text-sm">{c.excerpt}</p>{c.url&&<a href={c.url} target="_blank" rel="noopener noreferrer" className="text-sm underline">Open source</a>}</article>)}{!result.citations.length&&<p>No matching context returned.</p>}</section>}</form>;}
