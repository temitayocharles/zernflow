"use client";
import {useState} from 'react';import type {GatewayConversationControl} from '@/lib/social-gateway/types';
export function CollaborationControls({id,isOwner}:{id:string;isOwner:boolean}){const [busy,setBusy]=useState(false);const [message,setMessage]=useState<string|null>(null);const [control,setControl]=useState<GatewayConversationControl|null>(null);
 async function act(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const input=new FormData(event.currentTarget);setBusy(true);setMessage(null);try{const r=await fetch(`/api/v1/conversations/${id}/control`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(input))});const data=await r.json();if(!r.ok)throw new Error(data.error);setControl(data.control);setMessage(data.warning??'Gateway confirmed the control change.');}catch(e){setMessage(e instanceof Error?e.message:'Control request failed');}finally{setBusy(false);}}
 return <details className="rounded border border-border p-2 text-xs"><summary>Human / agent collaboration</summary><p className="my-2 text-muted-foreground">Controls execute through Gateway. Current control state is not loaded by this deployment; a response below is the last confirmed change, not a live presence indicator.</p><form onSubmit={act} className="flex flex-wrap gap-2"><label>Action<select name="action" className="ml-2 rounded border border-border bg-background p-1"><option value="assign_self">Assign to me</option><option value="unassign">Unassign</option><option value="escalate">Escalate</option>{isOwner&&<><option value="takeover">Human takeover</option><option value="release">Release to agent</option><option value="assign_agent">Assign agent</option></>}</select></label><label>Reason<input name="reason" maxLength={2000} className="ml-2 rounded border border-border bg-background p-1"/></label>{isOwner&&<label>Gateway agent reference<input name="agentRef" maxLength={200} className="ml-2 rounded border border-border bg-background p-1"/></label>}<button disabled={busy} className="rounded border border-border px-2 disabled:opacity-50">{busy?'Applying…':'Apply'}</button></form>{message&&<p role="status" className="mt-2">{message}</p>}{control&&<p className="mt-2">Last confirmed: {control.assignment_type} · {control.assignee_ref??'Unassigned'} · Takeover {control.human_takeover?'on':'off'} · Escalated {control.escalated?'yes':'no'} · Version {control.version}</p>}<p className="mt-2 text-muted-foreground">Autonomy: Allow / Ask / Deny / Limit are Gateway-owned policies. Approval queue and policy editing are unavailable until scoped read/write contracts are verified. No local setting bypasses Gateway approval.</p></details>;
}
